# 个人资料 - test

## 基本资料

- 姓名：CI 测试资料
- 学号：123
- 专业：计科
- GitHub：`test`（仅作 CI 格式测试，CI 不校验该用户是否存在）
- 意向方向：GitHub 协作与 CI 流程验证
- 技术基础：了解 Git 分支、提交和 Pull Request 的基本流程；了解 GitHub Actions 的触发器和只读权限配置。

## 关于我

### 为什么想要加入技术组？

测试

### 最近一次主动学习某项新东西的经历

- 学习内容：GitHub Actions 的 Pull Request 事件与 `pull_request_target` 触发器。
- 学习动机：需要为来自 Fork 的报名 PR 检查格式，同时避免执行提交者提供的代码。
- 学习过程与结果：阅读 GitHub Actions 文档后，使用只读权限获取 PR 元数据、提交和变更文件列表，并将检查限制为分支、标题和目录规则。

### 感兴趣的技术方向

对开发协作自动化感兴趣。小而明确的 CI 检查可以把重复的人工确认前置，让维护者专注于资料本身的质量。

### 平时如何使用 AI？

会用 AI 协助理解陌生配置、生成验证思路和审查边界条件；涉及权限、触发器或安全问题时，会再查阅官方文档确认。

### 如果 AI 给出的答案明显不对，你会怎么处理？

先将答案拆成可验证的断言，再用文档、最小示例和实际命令逐项确认；发现错误后会缩小问题范围，补充上下文重新提问。

## 技术资源

- 资源名称：GitHub Actions 工作流语法
- 链接：https://docs.github.com/zh/actions/writing-workflows/workflow-syntax-for-github-actions
- 我是如何找到它的：从 GitHub Docs 中搜索 `workflow syntax`，并沿着 Actions 文档导航进入该页面。
- 为什么值得参考：这是官方定义工作流触发器、权限和表达式语法的文档，适合在配置 CI 前核对行为。

## AI 辅助记录

### 1. 我遇到的问题

需要让 CI 检查来自 Fork 的 PR 标题、提交标题和改动目录，又不能检出或运行 PR 中的代码。

### 2. 我是怎么向 AI 提问的

“如何为来自 Fork 的 GitHub PR 校验元数据和提交格式，同时避免执行不受信任代码？需要哪些最小权限？”

### 3. AI 的回答有没有直接解决？

AI 给出了使用 PR 事件和 GitHub API 的思路，但仍需要查阅官方文档确认 `pull_request_target` 的执行上下文和权限边界。

### 4. 如果没有，我后来怎么继续处理？

结合官方文档，将工作流限制为只读权限，不使用 checkout，并只调用读取 PR 元数据、提交和文件列表的 API；随后用合法和不合法的本地测试数据验证反馈。
